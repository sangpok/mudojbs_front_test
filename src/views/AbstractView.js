export default class {
    constructor(urlParams = null, queryParams = null) {}

    setTitle = (newTitle) => {
        document.title = newTitle;
    };

    init = () => {};

    async getView() {}
}
